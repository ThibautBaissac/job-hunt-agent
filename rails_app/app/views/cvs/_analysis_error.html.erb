<section class="space-y-3 rounded-lg border border-rose-200 bg-rose-50 p-4">
  <header class="flex items-center gap-3">
    <span class="rounded-full bg-rose-200 px-2 py-0.5 text-xs font-semibold uppercase tracking-wide text-rose-900">Erreur</span>
    <h3 class="text-sm font-semibold text-rose-900">Analyse impossible</h3>
  </header>
  <p class="text-sm leading-6 text-rose-900"><%= message %></p>
  <p class="text-xs text-rose-700">Réessayez ou modifiez votre CV avant de relancer l’analyse.</p>
  <div id="<%= dom_id(cv, :analysis_stream) %>" class="hidden"></div>
</section>
